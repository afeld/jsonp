# initially generated with `heroku docker:init`
nginx:
  build: ./nginx
  links:
    - web
  ports:
    - '80:80'
web:
  build: .
  command: "bash -c 'npm start'"
  working_dir: /app/user
  environment:
    PORT: 8080
  ports:
    - '8080:8080'
